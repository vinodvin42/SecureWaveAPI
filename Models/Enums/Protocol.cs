namespace SecureWaveAPI.Models.Enums
{
    public enum Protocol
    {
        SSH,
        RDP,
        HTTP,
        HTTPS,
        FTP,
        SFTP,
        MySQL,
        PostgreSQL,
        MongoDB,
        SMTP,
        TCP,
        UDP
    }
}
